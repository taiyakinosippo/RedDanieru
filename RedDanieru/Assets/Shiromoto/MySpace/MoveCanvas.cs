using UnityEngine;

public class MoveCanvas : MonoBehaviour
{
    private static MoveCanvas instance;

    void Awake()
    {
        if (instance != null)
        {
            Destroy(gameObject);
            return;
        }

        instance = this;
        DontDestroyOnLoad(gameObject);
    }
}
